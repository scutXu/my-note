n次被贝塞尔曲线需要n+1个控制点

贝塞尔曲线方程:
1次:
B(t) = ((1 - t) * P0) + (t * P1), 0 <= t <= 1
2次:
B(t) = (pow((1 - t), 2) * P0) + (2 * (1 - t) * t * P1) + (pow(t, 2) * P2)

过3点的2次贝塞尔曲线:
正常的2次贝塞尔曲线是不过P1点的，为了得到过3点的曲线，需要求一个新的控制点Pc，使得
P0,Pc,P2的贝塞尔曲线过P1