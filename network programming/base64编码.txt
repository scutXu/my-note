目的
避免文件数据的字符被误解释为网络文本协议的一部分。

编码方式
1）将字节流拆分成6bit一个单元
2）对于每个单元，可表示为0-63的数值
3）对照映射表，将每个数值转换为一个字符
4）将字符转换回8位字节

FAQ
1)使用文本协议传输文件是否一定要经过base64编码
否，例如使用multipart/form-data上传文件时，需要定义分隔符，分隔符之间的数据解析为一份文件

2)在http协议中，mutlpart是否能够完全替代base64的作用，起到减少数据传输量的作用